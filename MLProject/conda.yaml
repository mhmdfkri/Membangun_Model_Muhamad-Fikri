name: telco-env

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      - mlflow
      - dagshub
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - joblib

